import React from "react";
import styled from "styled-components";



type JoinedDataTablePageProps = {
  template: React.ReactNode;
};

const JoinedDataTablePageWrapper = styled.div`
  
`;

const JoinedDataTablePage = ({}: JoinedDataTablePageProps) => {
  return (
    <JoinedDataTablePageWrapper>
      test2
    </JoinedDataTablePageWrapper>
  );
};

export default JoinedDataTablePage;