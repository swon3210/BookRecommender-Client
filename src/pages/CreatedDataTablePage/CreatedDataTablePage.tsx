import React from "react";
import styled from "styled-components";



type CreatedDataTablePageProps = {
  template: React.ReactNode;
};

const CreatedDataTablePageWrapper = styled.div`
  
`;

const CreatedDataTablePage = ({}: CreatedDataTablePageProps) => {
  return (
    <CreatedDataTablePageWrapper>
      test2
    </CreatedDataTablePageWrapper>
  );
};

export default CreatedDataTablePage;
